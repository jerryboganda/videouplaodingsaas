# Server
PORT=5173
SESSION_SECRET=change_me_to_a_long_random_string

# Admin account
ADMIN_USERNAME=admin
# Provide either ADMIN_PASSWORD (for first run) OR ADMIN_PASSWORD_HASH (bcrypt)
ADMIN_PASSWORD=change_me
# ADMIN_PASSWORD_HASH=

# 2FA email destination
ADMIN_2FA_EMAIL=mindreader420123@gmail.com

# SMTP (for sending OTP + reset emails)
# Use a real SMTP provider (Gmail App Password, SendGrid SMTP, Mailgun SMTP, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM="Secure Dashboard <no-reply@example.com>"

# Base URL used in password reset emails (adjust if deployed)
APP_BASE_URL=http://localhost:5173
